% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/results.R
\name{get_tweedspot_results}
\alias{get_tweedspot_results}
\title{Extract TweedSpot results as a tidy data frame}
\usage{
get_tweedspot_results(
  input,
  sort_by = c("padj", "none", "pval", "stat", "dev_expl"),
  decreasing = NULL
)
}
\arguments{
\item{input}{A \link[SpatialExperiment:SpatialExperiment]{SpatialExperiment::SpatialExperiment} containing TweedSpot
results in \code{rowData(input)}.}

\item{sort_by}{Column to sort by. Options are \code{"none"}, \code{"padj"}, \code{"pval"},
\code{"stat"}, or \code{"dev_expl"}. Default \code{"padj"}.}

\item{decreasing}{Logical; override the default sort direction for
\code{sort_by}. If \code{NULL}, p-values sort ascending and statistics/effect-size
columns sort descending.}
}
\value{
A \code{data.frame} containing the original \code{rowData(input)} columns and
TweedSpot result columns.
}
\description{
\code{get_tweedspot_results()} pulls the TweedSpot result columns from
\code{rowData(input)} into a base \code{data.frame} and can optionally sort the output.
}
\examples{
counts <- matrix(c(1, 2, 3, 4, 2, 2), nrow = 2, byrow = TRUE)
rownames(counts) <- c("gene1", "gene2")
colnames(counts) <- paste0("spot", seq_len(ncol(counts)))
spe <- SpatialExperiment::SpatialExperiment(
  assays = list(counts = counts),
  rowData = S4Vectors::DataFrame(
    gene_id = rownames(counts),
    gene_name = rownames(counts),
    tweedspot_stat = c(4, 2),
    tweedspot_pval = c(0.01, 0.05),
    tweedspot_padj = c(0.02, 0.05),
    tweedspot_edf = c(2, 2),
    tweedspot_dev_expl = c(0.3, 0.1)
  ),
  colData = S4Vectors::DataFrame(sample_id = rep("sample1", ncol(counts))),
  spatialCoords = cbind(x = seq_len(ncol(counts)), y = seq_len(ncol(counts)))
)
get_tweedspot_results(spe)
}
