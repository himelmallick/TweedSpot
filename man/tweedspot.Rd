% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tweedspot.R
\name{tweedspot}
\alias{tweedspot}
\title{Detect spatially variable genes with self-adaptive spatial Tweedie models}
\usage{
tweedspot(
  input,
  assay_name = "counts",
  covariates = NULL,
  two_part = FALSE,
  combine = c("CCT", "stouffer", "min"),
  family = "tw",
  fit_method = "REML",
  use_bam = FALSE,
  bam_discrete = TRUE,
  bam_nthreads = 1L,
  smooth_k = NULL,
  padj_method = "BY",
  BPPARAM = BiocParallel::bpparam(),
  verbose = TRUE
)
}
\arguments{
\item{input}{A \link[SpatialExperiment:SpatialExperiment]{SpatialExperiment::SpatialExperiment} with a count assay and
spatial coordinates.}

\item{assay_name}{Name of the count assay to model. Default \code{"counts"}.}

\item{covariates}{Optional one-sided formula specifying columns from
\code{colData(input)} to adjust for, for example \code{~ batch + age + sex}.
Character vectors of \code{colData(input)} column names are also supported.}

\item{two_part}{Logical; if \code{TRUE}, fit the two-part (hurdle) model (Tweedie
on counts plus a binomial model on presence/absence) and combine the two
p-values. Default \code{FALSE}.}

\item{combine}{P-value combination rule for the two-part model: \code{"CCT"}
(Cauchy), \code{"stouffer"}, or \code{"min"}. Default \code{"CCT"}.}

\item{family}{\code{mgcv} family for the count model. Default \code{"tw"} (Tweedie).}

\item{fit_method}{Smoothing parameter estimation method passed to \code{mgcv},
e.g. \code{"REML"} (default) or \code{"GCV.Cp"}.}

\item{use_bam}{Logical; use \code{\link[mgcv:bam]{mgcv::bam()}} instead of \code{\link[mgcv:gam]{mgcv::gam()}} for
scalability on large datasets. Default \code{FALSE}.}

\item{bam_discrete}{Logical; when \code{use_bam = TRUE}, use discretized fitting
via \code{mgcv::bam(discrete = TRUE)} for speed. Default \code{TRUE}.}

\item{bam_nthreads}{Integer; number of threads for \code{mgcv::bam()}. Default 1.}

\item{smooth_k}{Optional basis dimension for the spatial smooth. Smaller
values can speed fitting at the cost of flexibility. Default \code{NULL}, which
uses \code{mgcv}'s default.}

\item{padj_method}{Multiple-testing correction passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}.
Default \code{"BY"}.}

\item{BPPARAM}{A \link[BiocParallel:BiocParallelParam]{BiocParallel::BiocParallelParam} object controlling
parallelization across genes. Default \code{\link[BiocParallel:bpparam]{BiocParallel::bpparam()}}, which
uses the currently registered BiocParallel backend.}

\item{verbose}{Logical; if \code{TRUE}, print lightweight progress messages before
and after gene-wise model fitting. Default \code{TRUE}.}
}
\value{
The input \code{SpatialExperiment} with per-gene statistic, p-value,
adjusted p-value, smooth effective degrees of freedom, and deviance
explained written to \code{\link[SummarizedExperiment:rowData]{SummarizedExperiment::rowData()}}.
}
\description{
\code{tweedspot()} fits a Tweedie GAM with a spatial smooth \code{s(x, y)} per gene
and tests each gene for overall spatial variation.
}
\examples{
counts <- matrix(
  c(0, 1, 2, 3, 4, 5, 5, 4, 5, 4, 3, 2),
  nrow = 2,
  byrow = TRUE
)
rownames(counts) <- c("gene1", "gene2")
colnames(counts) <- paste0("spot", seq_len(ncol(counts)))
spe <- SpatialExperiment::SpatialExperiment(
  assays = list(counts = counts),
  rowData = S4Vectors::DataFrame(
    gene_id = rownames(counts),
    gene_name = rownames(counts)
  ),
  colData = S4Vectors::DataFrame(batch = rep(c("A", "B"), length.out = ncol(counts))),
  spatialCoords = cbind(x = seq_len(ncol(counts)), y = seq_len(ncol(counts)))
)
spe <- tweedspot(spe, family = "poisson", smooth_k = 4, BPPARAM = BiocParallel::SerialParam())
SummarizedExperiment::rowData(spe)[, c("tweedspot_stat", "tweedspot_pval")]
}
