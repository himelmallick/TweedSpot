% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_spatial_fit}
\alias{plot_spatial_fit}
\title{Plot the fitted spatial signal for a gene}
\usage{
plot_spatial_fit(
  input,
  gene,
  assay_name = "counts",
  covariates = NULL,
  family = "tw",
  fit_method = "REML",
  use_bam = FALSE,
  bam_discrete = TRUE,
  bam_nthreads = 1L,
  smooth_k = NULL,
  point_size = 1.8
)
}
\arguments{
\item{input}{A \link[SpatialExperiment:SpatialExperiment]{SpatialExperiment::SpatialExperiment} with a count assay and
spatial coordinates.}

\item{gene}{Gene identifier, row name, or row index to plot.}

\item{assay_name}{Name of the assay to model. Default \code{"counts"}.}

\item{covariates}{Optional one-sided formula specifying columns from
\code{colData(input)} to adjust for, for example \code{~ batch + age + sex}.
Character vectors of \code{colData(input)} column names are also supported.}

\item{family}{\code{mgcv} family for the count model. Default \code{"tw"}.}

\item{fit_method}{Smoothing parameter estimation method passed to \code{mgcv}.
Default \code{"REML"}.}

\item{use_bam}{Logical; use \code{\link[mgcv:bam]{mgcv::bam()}} instead of \code{\link[mgcv:gam]{mgcv::gam()}}. Default
\code{FALSE}.}

\item{bam_discrete}{Logical; when \code{use_bam = TRUE}, use discretized fitting.
Default \code{TRUE}.}

\item{bam_nthreads}{Integer; number of threads for \code{mgcv::bam()}. Default 1.}

\item{smooth_k}{Optional basis dimension for the spatial smooth. Default
\code{NULL}.}

\item{point_size}{Point size for the scatter plot. Default \code{1.8}.}
}
\value{
A \code{ggplot2} object.
}
\description{
\code{plot_spatial_fit()} refits the TweedSpot gene-level GAM for a single gene
and visualizes the fitted spatial smooth contribution across spatial
locations.
}
\examples{
\dontrun{
plot_spatial_fit(spe, gene = 1)
}
}
