% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/results.R
\name{top_spatial_genes}
\alias{top_spatial_genes}
\title{Return the top TweedSpot hits}
\usage{
top_spatial_genes(
  input,
  n = 10L,
  rank_by = c("padj", "pval", "stat", "dev_expl")
)
}
\arguments{
\item{input}{A \link[SpatialExperiment:SpatialExperiment]{SpatialExperiment::SpatialExperiment} containing TweedSpot
results in \code{rowData(input)}.}

\item{n}{Number of rows to return. Default \code{10}.}

\item{rank_by}{Ranking column. Options are \code{"padj"}, \code{"pval"}, \code{"stat"}, or
\code{"dev_expl"}. Default \code{"padj"}.}
}
\value{
A ranked \code{data.frame} containing the top TweedSpot hits.
}
\description{
\code{top_spatial_genes()} returns the top \code{n} rows from
\code{\link[=get_tweedspot_results]{get_tweedspot_results()}} using a user-selected ranking metric.
}
\examples{
counts <- matrix(c(1, 2, 3, 4, 2, 2), nrow = 2, byrow = TRUE)
rownames(counts) <- c("gene1", "gene2")
colnames(counts) <- paste0("spot", seq_len(ncol(counts)))
spe <- SpatialExperiment::SpatialExperiment(
  assays = list(counts = counts),
  rowData = S4Vectors::DataFrame(
    gene_id = rownames(counts),
    gene_name = rownames(counts),
    tweedspot_stat = c(4, 2),
    tweedspot_pval = c(0.01, 0.05),
    tweedspot_padj = c(0.02, 0.05),
    tweedspot_edf = c(2, 2),
    tweedspot_dev_expl = c(0.3, 0.1)
  ),
  colData = S4Vectors::DataFrame(sample_id = rep("sample1", ncol(counts))),
  spatialCoords = cbind(x = seq_len(ncol(counts)), y = seq_len(ncol(counts)))
)
top_spatial_genes(spe, n = 1)
}
